<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iffly.compose.redux">

    <application
        android:name="com.iffly.compose.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Compose_redux">
        <activity
            android:name="com.iffly.compose.mvi.MVIActivity"
            android:exported="true" />
        <activity
            android:name=".ReduxActivity"
            android:exported="true" />
        <activity
            android:name="com.iffly.compose.mvvm.MVVMActivity"
            android:exported="true" />
        <activity
            android:name="com.iffly.compose.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Compose_redux">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>